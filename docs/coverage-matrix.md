# Workstation coverage matrix

This matrix is the publication contract. **Observed** means the component was selected from the workstation during the allowlisted source audit. **Portable adaptation** means machine-bound or unsafe details were replaced while preserving the public behavior. **Recommendation** means the repository documents a reproducible architecture but does not claim to copy the live machine verbatim.

| Workstation component | Classification | Public implementation | Sanitization boundary | Verification | Deliberate exclusion or remaining limit |
|---|---|---|---|---|---|
| Hyprland desktop | Observed + portable adaptation | `dotfiles/hypr/.config/hypr/`: layout, animation, input, keybindings, lock, wallpaper loader and helper scripts | Generic monitor selection; monitor, input and GPU fragments rendered per machine from hardware facts, never committed; private wallpaper omitted | Shell syntax for helpers; repository privacy scan; two-pass Stow regression | Monitor identifiers, workspace/application history and private wallpaper are local-only |
| Waybar | Observed + portable adaptation | `dotfiles/waybar/.config/waybar/config` and `style.css` | Modules use portable commands and contain no host/device identifiers | JSON parser, privacy scan and isolated deployment | Machine-specific sensor/device modules are not fabricated |
| Kitty and Dunst | Observed + portable adaptation | `dotfiles/kitty/.config/kitty/kitty.conf`; `dotfiles/dunst/.config/dunst/dunstrc` | Palette and behavior retained without histories or private paths | Privacy scan and isolated deployment | Runtime rendering requires a graphical session |
| Rofi launcher and power menu | Observed + portable adaptation | Rofi config, `themes/bad-romance.rasi` and reviewed power-menu script | No application history or machine inventory | ShellCheck/syntax for script; privacy scan; isolated deployment | Menu execution is not exercised during headless tests because actions can end the session |
| dmenu | Observed portable entry point | `SUPER+D` invokes `dmenu_run` with the workstation font and bad-romance colors in `hyprland.conf` | No dmenu cache/history is imported | Manifest contains `dmenu`; command is present in deployed Hyprland config | dmenu has no private state or separate config file to publish |
| Wofi | Observed portable entry point | `SUPER+ALT+R` plus `dotfiles/wofi/.config/wofi/style.css` | No launcher history | Manifest, privacy scan and isolated deployment | Headless test does not open the Wayland UI |
| Neovim/NvChad skin | Observed + portable adaptation | Complete `dotfiles/nvim/.config/nvim`, `penumbra_dark` theme declaration, mappings, options, LSP, completion and locked plugins | Embedded legacy credential removed; paths use environment/PATH; no histories, caches or provider tokens | Every Lua file compiles; clean isolated Lazy install; startup assertions load NvChad, Treesitter, Avante and cmp | Provider requests are intentionally not made; they require separately authenticated local/cloud services |
| Neovim async path completion | Observed + availability adaptation | `FelipeLema/cmp-async-path` with `async_path`, pinned in `lazy-lock.json` | Upstream Codeberg URL in NvChad is overridden by the author's verified GitHub mirror | Clean clone checks exact GitHub origin and module/plugin installation | Functionality is retained; only the transport mirror changes |
| Avante AI workflow | Observed + portable adaptation | Avante UI, provider switching, per-tab agents and local context modules | No keys; loopback endpoints are overridable environment defaults | Native build completes during clean Lazy sync; `require("avante")` assertion | External AI responses are outside editor smoke-test scope |
| Interactive shell | Portable safety adaptation | Minimal `.bashrc` with editor, pager, completion, prompt and Arch maintenance aliases | Private endpoints, credentials and personal command history excluded | Bash syntax, ShellCheck, privacy scan and isolated deployment | It is intentionally not a verbatim dump of all private shell integrations |
| GTK/KDE theme | Observed + portable adaptation | GTK 3/4 settings and `kdeglobals`, coordinated with desktop/launcher/editor palette | No private icon or wallpaper assets | Structured/text checks, privacy scan and isolated deployment | Visual fidelity still requires installed fonts/icons and a graphical smoke test |
| PipeWire audio | Observed + portable adaptation | PipeWire/Pulse rate and stability fragments; `configure-audio.py`; optional profile templates | Device serials and node names become destination-local inputs | Python parsing, privacy scan and isolated deployment | No active audio daemon is restarted during tests |
| Bluetooth audio/policy | Observed + safer portable adaptation | WirePlumber balanced profile, optional gaming profile and sanitized BlueZ system config | MAC addresses and device identities omitted; permissive repair policy changed to confirmation | Config/privacy checks; `apply-system.sh --dry-run` | Pairing and reconnection need destination hardware; no active Bluetooth service is changed in tests |
| ClamAV and security audits | Observed + portable adaptation | FreshClam/clamd configs, weekly user timer/service, `workstation-security-audit`, UFW policy and documented Lynis/rkhunter use | Scan targets derive from destination HOME; no local logs or firewall rules copied | systemd unit verification, shell checks, privacy/Gitleaks scan; system apply remains dry-run | Signatures, scan output and existing allow rules are runtime/local state |
| Disk architecture | Recommendation derived from observed pattern | `docs/disk-architecture.md`: Btrfs system/HOME, Linux data tier, optional shared tier, LUKS2 and placeholder fstab | No device names, capacities, labels, UUIDs or exact mount layout | Documentation/privacy review only; no partition or mount operation is performed | This is explicitly not a disk image or unattended partitioner |
| Services and automations | Observed functions + generic parameterization | Hyprland session daemons, ClamAV timer, FreshClam, Bluetooth, UFW and fstrim declarations plus `workstation-task@` service/timer templates and JSON runner | Personal endpoints, arguments, credentials and device identities become destination-local task data | systemd verification, runner syntax/JSON checks, dry-run privileged plan and isolated user deployment | Physical-device services still require destination hardware; their reusable process lifecycle is represented rather than fabricated |
| Language axes | Portable implementation | `lib/settings.sh`, `apply-system.sh --locale/--keymap`, `render/hypr/generated/input.conf.in` | Locale, console keymap and XKB layout read from one user settings file; defaults reproduce the workstation (`es_ES.UTF-8`, `es`, `es`) | Single-writer test per axis, no-global-`LC_ALL` test, VM sets three distinguishable values and asserts each file independently | The settings file is never committed |
| Graphical login | Portable opt-in implementation | `greetd`/`tuigreet` package profile and generic Hyprland session configuration | No usernames, credentials or machine display-manager state; only the disposable VM variant autologs its throwaway guest user | Package resolution, TOML parsing and real Arch VM installation | Not enabled by default on an existing host; VM/manual profile makes the complete login path explicit |
| Virtual machine | Portable verification implementation | `scripts/test-vm.sh`, official checksummed Arch cloud image, KVM/QEMU overlay, NoCloud seed and optional graphical mode | Ephemeral key, guest logs and disk stay in ignored `.vm-test/`; network binds only to loopback | Real guest boot, package installation, Stow deployment, checker, clean Neovim test, Hyprland verification and executable assertions | Cannot simulate physical Bluetooth/audio/GPU/disk behavior; these remain destination tests |
| Package manifests | Curated portable adaptation | Base `pacman.txt`/`aur.txt` plus composable `desktop-login.txt` and `vm.txt` | Curated functional dependencies, not a personal application inventory | Sorted/unique check; profile dry-runs; isolated Arch resolution and VM installation | Host-specific software remains local unless its reusable function has a public profile |
| Deployment and backup behavior | Portable implementation | Stow packages plus conflict backup under XDG state | Operates only on allowlisted package files | Automated two-pass temporary-HOME test hashes every source file and confirms one conflict backup; the deploy-integrity gate deploys Stow and the machine-specific renders into a sandbox and asserts no path is claimed by both, every link resolves into `dotfiles/`, every render is a regular file outside the checkout, and the checkout is byte-identical afterwards | Tests never deploy to the real HOME |

## GPU families

The catalogue (`data/gpu-catalogue.tsv`) maps PCI ids to driver families and each family to a package manifest. A family is **Observed** only where someone has run it; everything else is a recommendation assembled from the Arch wiki and untested on hardware. The status words below are read by `scripts/check-gpu-catalogue.sh`, which fails when this table and the catalogue disagree.

| Family | Matches | Manifest | Status |
|---|---|---|---|
| `nvidia_open` | NVIDIA, device ids 1e00 and up (Turing and newer) | `packages/gpu-nvidia_open.txt` | Observed on the author's workstation |
| `nvidia_proprietary` | NVIDIA, device ids 1340 to 1dff (Maxwell, Pascal) | `packages/gpu-nvidia_proprietary.txt` | Recommendation, untested on hardware |
| `nvidia_legacy_470` | NVIDIA, device ids 0f00 to 133f (Kepler); packages come from the AUR | `packages/gpu-nvidia_legacy_470.txt` | Recommendation, untested on hardware |
| `intel_xe` | Intel, Lunar Lake, Battlemage and Panther Lake id ranges | `packages/gpu-intel_xe.txt` | Recommendation, untested on hardware |
| `intel_i915` | Any other Intel GPU | `packages/gpu-intel_i915.txt` | Recommendation, untested on hardware |
| `amd_amdgpu` | Any AMD GPU | `packages/gpu-amd_amdgpu.txt` | Recommendation, untested on hardware |
| `virtio` | virtio GPU | `packages/gpu-virtio.txt` | VM-verified |
| `generic` | Anything else | `packages/gpu-generic.txt` | Recommendation, untested on hardware |

Hybrid machines (two families at once) install both families plus `packages/gpu-prime-offload.txt` (`nvidia-prime`): the integrated GPU drives the displays and the compositor receives no NVIDIA environment; programs run on the NVIDIA GPU through `prime-run`. Status: Recommendation, untested on hardware. `scripts/gpu-setup.sh` resolves and installs the stack (dry run by default; `--list` prints this table's status from the catalogue). When any family installs a DKMS module, the headers of every installed kernel are added (`linux-headers`, `linux-zen-headers`, ...). With Secure Boot enabled and a DKMS module in the stack, the tooling prints a warning naming the consequence and the two ways out (enrol a key with `sbctl`, or disable Secure Boot); it neither installs silently nor refuses.

## Release interpretation

The repository reproduces the public workstation layers listed above. It does **not** claim that excluded personal services, application data, histories, credentials, device identities, private media or exact storage hardware are reproduced. Graphical, hardware and privileged effects remain destination-side acceptance tests and are never simulated as if they had run.
