# English strings. English is the reference language: every other table is
# checked against this key set, and a key missing elsewhere falls back to the
# value here, visibly marked. See docs/i18n-schema.md.
# keys: 14
selector.desktop_login=Graphical login (greetd and tuigreet)
selector.vm=QEMU/SPICE guest integration
selector.not_applicable={1} does not apply to this machine: it needs {2}
selector.applies=applies
selector.needs=not applicable (needs {1})
bootstrap.this_machine=this machine: chassis={1} gpu={2}
gpu.nvidia_open=NVIDIA, open kernel modules (Turing and newer)
gpu.nvidia_proprietary=NVIDIA, proprietary driver (Maxwell and Pascal)
gpu.nvidia_legacy_470=NVIDIA, legacy 470 branch (Kepler, from the AUR)
gpu.intel_xe=Intel, xe driver (Lunar Lake, Battlemage, Panther Lake)
gpu.intel_i915=Intel, i915 driver
gpu.amd_amdgpu=AMD, amdgpu driver
gpu.virtio=Virtual GPU (virtio)
gpu.generic=Unknown GPU, Mesa only
